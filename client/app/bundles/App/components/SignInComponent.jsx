import PropTypes from 'prop-types';
import React from 'react';

const SignIn = ({}) => (
  <div> kajsdlkasjdklsad </div>
)

SignIn.propTypes = {
  //field: PropTypes.string.isRequired,
  //handler: PropTypes.func.isRequired,
};

export default SignIn;